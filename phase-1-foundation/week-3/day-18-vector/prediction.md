1.  When a matrix multiplied by a vector gives a new vector, then is the matrix moving the vector, rotating it, scaling it, or something else entirely?
- It is moving and scalling the vector, it can't rotate because retating is nothing but generating a new point so for that need addition

2. Every layer in a neural network is a matrix multiplication. Before watching: what do you think the matrix is "doing" to the input data at each layer?
- I think matric multiplication is for neural network, so when multiplied then may a new combined n/w but Idk when this matrix wrt space comes into picture whem discussed abour linear algebra

3. In Spark, a .map() applies a function to every row. A matrix multiplication also applies something to every vector. Predict: what's the key constraint on what a matrix transformation is allowed to do?
- between 2 matrix either no of rows or mo of columns should be same or one of the should have dim as 1.
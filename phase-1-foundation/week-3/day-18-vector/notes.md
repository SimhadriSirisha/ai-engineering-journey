Till now we simply saw matrix multiplying with vector as a simple mathematical operation but never understood the internal meaning. When a matrix is multiplies with a vector is determining the vector in transformed space. Transformed spae means visualizing the grid in different direction. 
Matrix values are the landing points of j-hat and j-hat. And any vector of magnitude value multiplied to the matrix gives basis of vectors in the transformed space. This formula is constant and works for every vector because of the linear transformation. Linear transformation because of grids are parallel and origin is remain at the same place and doesn't change.
One very interesting point is when basis vectors are dependent then its transformation brings the whole grid collapses to single line.
1. does A×B equal B×A for matrices? Test with any 2×2 examples in NumPy first. What did you find?
No its not same, 
let A = 2 X 2 & B = 2 X 1 
if A X B = 2 X 1 
& when B X A = invalid 

2. In a 3-layer neural network, you apply transformation A, then B, then C. Predict: is applying them one at a time the same as computing A×B×C first and then applying once? IDK

3. the determinant is a single number that summarises an entire matrix. what property of the transformation does it capture? Size? Speed? Something about areas? IDK, a wild gues size

4. Predict: what does a determinant of 0 mean geometrically? What has the transformation done to space? IDk, As I told size before, will go by that. When determinant = 0, that means size = 0, transformation has collapsed the space into single line.

5. When is it impossible to invert a transformation? if it collapses to single line then no inversion at all .

6. What does the null space mean for your retail data? all features are dependent so space collapses

7. Before watching: the dot product of two vectors is a single number. Predict — what does that number measure about the relationship between the two vectors? IDK

8. In Week 13 you'll build a semantic search engine using cosine similarity between embeddings. Before this video, predict: why would "similar" products have a high dot product value? mathematically i can explain, as dot product =  A.B = ABcos(theta), if similar theta = 0 & cos 0 = 1 so A.B = AB which is highest of all angles

9. Most vectors change direction when a matrix transforms them. What's special about the ones that don't?
when we keep the vectors axis contant

10. How could finding "directions that don't rotate" help compress a 100-column product dataset into 5 columns? IDK
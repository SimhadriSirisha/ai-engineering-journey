1. You're training an ML model and it's making wrong predictions. Gradient descent is how it fixes itself. Predict — how do you think it figures out which direction to adjust?
IDK what is gradient descent

2. A neural network has 10 layers. An error happens at the output layer. Predict — how does that error travel back to fix the weights in layer 1? No idea